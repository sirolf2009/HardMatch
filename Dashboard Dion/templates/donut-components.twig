<script>
Morris.Donut({
	element: 'components',
	data: [
	{% for store in stores %}
	{
	label: "{% for label in store['LABELS(n)'] %}{% if label != "Component" %}{{label}}{% endif %}{% endfor %}", 
	value: {{ store['COUNT(n)'] }}},
	{% endfor %}
	],
	resize: true
});
</script>
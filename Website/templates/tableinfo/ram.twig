<td>
    <table border="0">
        <tr>
            <td style="width:200px">Naam:</td>
            <td>{{ getNodeProperty(node, 'product') }}</td>
        </tr>
        <tr>
            <td>Type:</td>
            <td>{{ getNodeProperty(node, 'memoryType') }}</td>
        </tr>
        <tr>
            <td>Aantal:</td>
            <td>{{ getNodeProperty(node, 'Aantal') }}</td>
        </tr>
        <tr>
            <td>Geheugen CAS Latency:</td>
            <td>{{ getNodeProperty(node, 'GeheugenCASLatency') }}</td>
        </tr>
        <tr>
            <td>Merk:</td>
            <td>{{ getNodeProperty(node, 'brand') }}</td>
        </tr>
        <tr>
            <td>Artikelnummer:</td>
            <td>{{ getNodeProperty(node, 'ModelID') }}</td>
        </tr>
    </table>
</td>